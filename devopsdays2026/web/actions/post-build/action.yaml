name: Post Build Action

inputs:
  args_json:
    description: "Arguments for action"
    required: false
    default: "{}"
  merged_config_json:
    description: "Merged configuration JSON from previous steps"
    required: false
    default: "{}"

runs:
  using: "composite"
  steps:
    - name: Copy config Folders
      shell: bash
      run: |
        echo "This is a post-build action. You can add your custom logic here."